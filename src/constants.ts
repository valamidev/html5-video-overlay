export const DEFAULT_OUTER_DIV = { type: 'div', class: 'buff-outer-wrapper' };

export const DEFAULT_BUFF_INNER_OVERLAY = { type: 'div', class: 'buff-inner-overlay-wrapper' };

export const DEFAULT_BUFF_INNER_OVERLAY_HTML = '<div class="buff-inner-overlay-wrapper"></div>';

export const DEFAULT_FADE_TIME = 2000; // 2 sec in milliseconds

export const FIRST_REQUEST_DELAY = 500;

export const DEFAULT_API_URI = 'https://buffup.proxy.beeceptor.com';
